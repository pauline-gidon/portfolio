twig:
    form_themes:
        - 'form/custom_types_recaptcha.html.twig'
        - 'bootstrap_4_layout.html.twig'

    default_path: '%kernel.project_dir%/templates'

    globals:
        gg_recaptcha_site_key: '%env(GOOGLE_RECAPTCHA_SITE_KEY)%'
  
        
        

when@test:
    twig:
        strict_variables: true

